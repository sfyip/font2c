
/*
    <Generate from template_enc1b.ini>
    generate margin and bitmap offset
    font: ${font}
    size: ${size}
    encoding method: ${encoding_method}

    width: ${width}
    height: ${height}
*/

static const font_symbol_t ${font}${size}_symbol[] = {
====split====
    {${imgaddr}, ${imglen}},  // ${imgname}
====split====
};