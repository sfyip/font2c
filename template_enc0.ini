/*
    <Generate from template_enc0.ini>
    generate font bitmap
    font: ${font}
    size: ${size}
    encoding method: ${encoding_method}

    width: ${width}
    height: ${height}
*/

#include "font.h"

#define ${fontcaps}${size}_WIDTH     ${width}
#define ${fontcaps}${size}_HEIGHT    ${height}

const uint8_t ${font}${size}_bmp[][] = {
====split====
    {   // ${imgname}
    ${imgdata}
    },
====split====
};
//=================== End ======================